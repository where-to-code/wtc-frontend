import styled from 'styled-components';

export const StyledMap = styled.div`
  display: none;
  @media (min-width: 600px) {
    position: fixed;
    display: block;
    top: 32px;
    right: 0px;
    height: 100%;
  }
`;
